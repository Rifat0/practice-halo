(function () {
  var myElement = document.getElementById("simple-bar");
  new SimpleBar(myElement, { autoHide: true });
})();

// import SimpleBar from "simplebar"

// SimpleBar.instances.get(document.getElementById("simple-bar")).recalculate()
